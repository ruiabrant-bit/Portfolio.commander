import { Sparkles } from 'lucide-react';
import { PlaceholderPage } from '../components/PlaceholderPage';

export function AICommanderPage() {
  return (
    <PlaceholderPage
      icon={Sparkles}
      title="AI Commander"
      description="Ask about risk, allocation, concentration or where to deploy new capital — always with reasoning attached."
      commitLabel="Coming in Commit 007 — AI Commander"
    />
  );
}
